#include <iostream>

using namespace std;

int main() {

    int age, price;
    char initial;
    float rate, tax;

    rate = 0.008875;

    cin >> age >> price >> initial;

    // so mixed typed precedence = int cast to other types so 5 + 2.5 = 5.0 + 2.5 = 7.5

    taxed = price * rate;

    cout << age << " " << tax << " " << initial << endl;
    cout << 3/2 + 5.5 << endl;

    return 0;

}
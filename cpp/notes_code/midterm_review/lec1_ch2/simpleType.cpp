#include <iostream>

using namespace std;

int main() {

    /* the sizeof function takes in a data type or a data type identifier, then spits out how many
       bytes it takes up as int value
    */

    cout << sizeof(float) << endl;
    cout << sizeof(char) << endl;
    return 0;

}
#include <iostream>
#include <iomanip>
#include <fstream>
#include <cmath>
#include <string>

using namespace std;

int main() {

    int a, b;
    cout << "Enter an int: " << endl;
    cin >> a;

    if (a%2 == 1); // doesn't even have stmnt to exec, so this will always be true if odd
        cout << "Hello" << endl;
        cout << "World" << endl;


    return 0;

}